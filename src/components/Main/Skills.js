import * as React from "react";
import styled from "styled-components";

const Container = styled.section``;

const Heading = styled.h2``;

const Skills = () => {
    return (
        <Container>
            <Heading>Skills</Heading>
            <div>HTML, CSS, JS, React, SASS, Git, TDD</div>
        </Container>
    )
}

export { Skills };
